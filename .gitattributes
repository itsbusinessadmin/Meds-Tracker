# The ui-ux-pro-max skill records sha256 hashes of these files over their raw
# bytes, so a CRLF checkout would fail its data validation. Pin them to LF.
.claude/skills/ui-ux-pro-max/data/*.csv    text eol=lf
.claude/skills/ui-ux-pro-max/data/*.json   text eol=lf
