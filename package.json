{
  "name": "pillbox",
  "version": "2.0.0",
  "private": true,
  "description": "Pillbox — iOS medication reminder and supply tracking. Reference prototype of design-system/pillbox.",
  "type": "module",
  "scripts": {
    "audit": "node test/audit.mjs",
    "flows": "node test/flows.mjs",
    "test": "npm run audit && npm run flows"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
